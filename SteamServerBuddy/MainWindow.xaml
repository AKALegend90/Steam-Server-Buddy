<Window x:Class="SteamServerBuddy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SteamServerBuddy.ViewModels"
        xmlns:v="clr-namespace:SteamServerBuddy.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Steam Server Buddy" Height="700" Width="1100"
        Icon="icon.png"
        Background="#1A202C">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="#2D3748" BorderBrush="#4A5568" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Server Buddy" Foreground="White" FontSize="20" FontWeight="Bold" Margin="20,20,0,20"/>
                
                <!-- Navigation Buttons with Selection Highlight -->
                <StackPanel Grid.Row="1" x:Name="NavButtons">
                    <RadioButton GroupName="Navigation" Content="Dashboard" Tag="Dashboard" 
                                 Command="{Binding NavigateCommand}" CommandParameter="Dashboard"
                                 IsChecked="True">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="RadioButton">
                                            <Border x:Name="NavBorder" Padding="15,12" Background="Transparent" BorderThickness="0,0,0,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="ViewDashboard" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White"/>
                                                    <TextBlock Text="{TemplateBinding Content}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="NavBorder" Property="Background" Value="#4A5568"/>
                                                    <Setter TargetName="NavBorder" Property="BorderBrush" Value="#4299E1"/>
                                                    <Setter TargetName="NavBorder" Property="BorderThickness" Value="3,0,0,0"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="NavBorder" Property="Background" Value="#3A4558"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton GroupName="Navigation" Content="Servers" Tag="Servers" 
                                 Command="{Binding NavigateCommand}" CommandParameter="Servers">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="RadioButton">
                                            <Border x:Name="NavBorder" Padding="15,12" Background="Transparent" BorderThickness="0,0,0,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="ViewGrid" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White"/>
                                                    <TextBlock Text="{TemplateBinding Content}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="NavBorder" Property="Background" Value="#4A5568"/>
                                                    <Setter TargetName="NavBorder" Property="BorderBrush" Value="#4299E1"/>
                                                    <Setter TargetName="NavBorder" Property="BorderThickness" Value="3,0,0,0"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="NavBorder" Property="Background" Value="#3A4558"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton GroupName="Navigation" Content="Add Server" Tag="Add" 
                                 Command="{Binding NavigateCommand}" CommandParameter="Add">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="RadioButton">
                                            <Border x:Name="NavBorder" Padding="15,12" Background="Transparent" BorderThickness="0,0,0,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="PlusCircle" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White"/>
                                                    <TextBlock Text="{TemplateBinding Content}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="NavBorder" Property="Background" Value="#4A5568"/>
                                                    <Setter TargetName="NavBorder" Property="BorderBrush" Value="#4299E1"/>
                                                    <Setter TargetName="NavBorder" Property="BorderThickness" Value="3,0,0,0"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="NavBorder" Property="Background" Value="#3A4558"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>

                    <RadioButton GroupName="Navigation" Content="Settings" Tag="Settings" 
                                 Command="{Binding NavigateCommand}" CommandParameter="Settings">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="RadioButton">
                                            <Border x:Name="NavBorder" Padding="15,12" Background="Transparent" BorderThickness="0,0,0,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="Cog" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="White"/>
                                                    <TextBlock Text="{TemplateBinding Content}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="NavBorder" Property="Background" Value="#4A5568"/>
                                                    <Setter TargetName="NavBorder" Property="BorderBrush" Value="#4299E1"/>
                                                    <Setter TargetName="NavBorder" Property="BorderThickness" Value="3,0,0,0"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="NavBorder" Property="Background" Value="#3A4558"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Style>
                        </RadioButton.Style>
                    </RadioButton>
                </StackPanel>
                
                <!-- Footer Credit -->
                <TextBlock Grid.Row="3" Foreground="#718096" FontSize="11" HorizontalAlignment="Center" Margin="0,0,0,15" Opacity="0.6">
                    Created by 
                    <Hyperlink NavigateUri="https://github.com/AKALegend90" RequestNavigate="Hyperlink_RequestNavigate" Foreground="#4299E1" TextDecorations="None">
                        AKALegend90
                    </Hyperlink>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Content -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Margin="20"/>
    </Grid>
</Window>
