<UserControl x:Class="SteamServerBuddy.Views.ServersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:SteamServerBuddy.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">
    
    <!-- Global resources are in App.xaml -->

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Manage Servers" FontSize="28" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            <Button Command="{Binding RefreshCommand}" Content="Refresh" Margin="20,0,0,0"
                    Style="{DynamicResource MaterialDesignOutlinedButton}"/>
        </StackPanel>
        
        <!-- Loading -->
        <TextBlock Text="Loading..." Foreground="White" FontSize="16" 
                   Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                   HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0" Grid.Row="1"/>

        <!-- Server List -->
        <ListBox Grid.Row="1" ItemsSource="{Binding Servers}" Background="Transparent" BorderThickness="0" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="0,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ServerItemViewModel}">
                    <Border Background="#2D2D30" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/> <!-- Status Dot -->
                                <ColumnDefinition Width="*"/>  <!-- Name -->
                                <ColumnDefinition Width="Auto"/> <!-- Buttons -->
                            </Grid.ColumnDefinitions>

                            <!-- Status Dot -->
                            <Ellipse Grid.Column="0" Width="12" Height="12" Fill="{Binding StatusColor}" VerticalAlignment="Center"/>

                            <!-- Info -->
                            <Button Grid.Column="1" HorizontalAlignment="Left" Padding="10,5"
                                    Command="{Binding DataContext.OpenDetailCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Style="{DynamicResource MaterialDesignTextButton}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="White"/>
                                    <TextBlock Text="{Binding AppId, StringFormat='ID: {0}'}" FontSize="12" Foreground="White"/>
                                </StackPanel>
                            </Button>

                            <!-- Actions -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <!-- Removed Settings and Console buttons as requested -->

                                <Button Content="Folder" Command="{Binding OpenFolderCommand}"
                                        Style="{DynamicResource MaterialDesignTextButton}"
                                        Width="80" Height="30" Margin="5,0" Padding="0"/>
                                
                                <Button Content="Uninstall" Command="{Binding UninstallCommand}" IsEnabled="{Binding CanUninstall}"
                                        Style="{DynamicResource MaterialDesignTextButton}"
                                        Width="80" Height="30" Margin="5,0" Padding="0"/>

                                <Button Content="Start" Command="{Binding StartCommand}" IsEnabled="{Binding CanStart}"
                                        Style="{DynamicResource MaterialDesignRaisedButton}"
                                        Width="80" Height="30" Margin="5,0" Padding="0"/>
                                <Button Content="Stop" Command="{Binding StopCommand}" IsEnabled="{Binding CanStop}"
                                        Style="{DynamicResource MaterialDesignOutlinedButton}"
                                        Width="80" Height="30" Margin="5,0" Padding="0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
