<UserControl x:Class="SteamServerBuddy.Views.ConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,20" Visibility="{Binding IsEmbedded, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="{Binding ServerName, FallbackValue='Server Console'}" FontSize="28" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Live Log Output (server.log)" FontSize="14" Foreground="White"/>
            </StackPanel>
            <Button Command="{Binding ClearCommand}" Content="CLEAR" HorizontalAlignment="Right" VerticalAlignment="Top" Width="100"
                    Style="{DynamicResource MaterialDesignOutlinedButton}"/>
        </Grid>

        <!-- Console Text -->
        <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="5" Padding="10" BorderBrush="#333333" BorderThickness="1">
            <TextBox Text="{Binding LogContent}" 
                     IsReadOnly="True" 
                     AcceptsReturn="True" 
                     TextWrapping="Wrap"
                     Background="Transparent" 
                     Foreground="#DCDCDC" 
                     FontFamily="Consolas" 
                     FontSize="12"
                     BorderThickness="0"
                     VerticalScrollBarVisibility="Auto"
                     TextChanged="TextBox_TextChanged"/>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <CheckBox IsChecked="{Binding AutoScroll}" Content="Auto-scroll" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
